syntax = "proto3";
package chat;

// メッセージの中身を定義
message Message {
    string body = 1;
}

// コード生成に必要
option go_package="grpc-sample/chat";

service ChatService {
    // メソッドを定義
    rpc SayHello(Message) returns (Message) {}
}